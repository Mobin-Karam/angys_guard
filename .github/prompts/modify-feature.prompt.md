Modify this existing feature: <feature/change>

First locate the current implementation, callers, configuration/state, tests, docs, and security/privacy boundaries. Explain current behavior before editing.

Make the smallest coherent change that satisfies the new acceptance criteria while preserving unrelated behavior. Reuse existing abstractions and migration patterns. Add regression coverage for old behavior that must remain and tests for the new behavior/failure paths.

Run focused verification, then broader repository checks. Report compatibility/migration impact and any manual validation required.