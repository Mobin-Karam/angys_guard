Design and implement this API/interface change: <change>

Inspect the existing interface, all known callers/consumers, validation, authorization, error semantics, versioning/migration conventions, and tests.

Before editing, decide whether the change can be additive/backward-compatible. Prefer additive evolution over breaking replacement where practical.

Cover:
- input/output contract;
- validation and error behavior;
- auth/security/privacy;
- idempotency/retries/timeouts where relevant;
- old/new caller compatibility;
- migration/deprecation plan;
- tests for normal, invalid, unauthorized, boundary, and compatibility cases.

Update docs/specs/examples if the interface is user/developer facing.