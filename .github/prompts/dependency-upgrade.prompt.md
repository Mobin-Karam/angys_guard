Safely evaluate and perform this dependency upgrade: <dependency/change>

Inspect why/how the dependency is used, supported version constraints, release/security notes if available, transitive/platform implications, and existing tests.

Prefer the smallest compatible upgrade. Check breaking API/config/runtime changes, license/security implications, lock/requirements consistency, optional native/system dependencies, and rollback path.

Update code only where required by the new version, run focused/full tests and package/dependency checks, and document any new platform/runtime requirement.