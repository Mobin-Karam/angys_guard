Implement the smallest safe change for: <task>

Read applicable repository instructions and inspect the owning code/tests first. Avoid redesigning adjacent systems.

Constraints:
- change only what is necessary for the acceptance criteria;
- reuse existing APIs/abstractions;
- preserve compatibility and security/privacy gates;
- do not suppress errors merely to make tests pass;
- add the smallest focused regression tests that prove the behavior;
- avoid formatting/reorganization outside touched logic.

After editing, review the diff specifically for accidental scope expansion and run the relevant checks.