Write a commit message for the current staged/intended change.

Infer the dominant purpose from the actual diff. Follow repository commit conventions if present; otherwise use a concise imperative subject and an optional body only when it adds useful rationale.

Do not claim tests, fixes, or behavior not actually present. Mention security/migration implications in the body when material. Return only the commit message.